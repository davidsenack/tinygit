# tinygit

> Built with Claude Code in a sandboxed container.

## Getting Started

1. Open this project in VS Code
2. Click "Reopen in Container" when prompted
3. Run `claude` in the terminal

## Development

All work happens inside the container. Your files persist on your host
machine via bind mount. Shell history and Claude config persist in
Docker volumes.
